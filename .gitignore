# Build artifacts   (Ensure all files are stored safely, not all files are needed in the repo and may not be inside in the main VS code directory)
/dist
/out

# Dependencies
/node_modules

# VS Code
.vscode-test/
*.vsix
.vscode/  # Exclude entire .vscode folder except config files

# Security-sensitive files
.env
*.env
*.secret
credentials*.json

# System files
.DS_Store
Thumbs.db
desktop.ini

# Logs and temp files
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
tmp/
temp/

# VS Code packaging artifacts
webpack-assets.json